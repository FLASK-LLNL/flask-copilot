# Flask Copilot - Database Configuration Template
# 
# This is an EXAMPLE file to help you set up your own .env file.
# Copy this to .env and fill in your actual values:
#   cp .env.example .env
#
# The .env file is gitignored and will NOT be committed to version control.
# Each user/deployment should have their own .env file with real credentials.

# ============================================================================
# DATABASE CONNECTION (Required)
# ============================================================================

# Database username
# Example: DB_USER=myusername
DB_USER=

# Database password (REQUIRED - never commit this!)
# Example: DB_PASSWORD=my_secure_password_123
DB_PASSWORD=

# Database host
# - For SSH tunnel to LC LaunchIT MariaDB: 127.0.0.1
# - For local MariaDB: localhost or 127.0.0.1
# - For remote direct connection: hostname or IP address
DB_HOST=127.0.0.1

# Database port
# - For SSH tunnel to LC LaunchIT: 32636
# - For local MariaDB: 3306 (default)
# - For custom setup: your custom port
DB_PORT=32636

# Database name
# Example: DB_NAME=flaskcopilot
DB_NAME=flaskcopilot


# ============================================================================
# SSL/TLS CONFIGURATION (Optional)
# ============================================================================

# Path to CA certificate file for SSL/TLS verification
# Uncomment and set if you need certificate verification (production)
# Example: DB_SSL_CA=/path/to/ca-cert.pem
# DB_SSL_CA=

# Enable strict SSL certificate verification
# Set to 'true' for production with proper certificates
# Set to 'false' or leave commented for dev/SSH tunnels
# DB_SSL_VERIFY=false


# ============================================================================
# SQLITE FALLBACK (Optional)
# ============================================================================

# Enable SQLite fallback mode (local file-based database)
# Uncomment to use SQLite instead of MariaDB (useful for offline development)
# USE_SQLITE_FALLBACK=1

# SQLite database file path
# Only used if USE_SQLITE_FALLBACK=1
# SQLITE_PATH=sqlite:///flaskcopilot.db


# ============================================================================
# TESTING (Optional)
# ============================================================================

# Timeout in seconds for database connection tests (test_remote_db.py)
# Default: 60 seconds
# DB_TEST_TIMEOUT=60
