import React from 'react';
import ReactDOM from 'react-dom/client';
import ChemistryTool from './App';

const root = ReactDOM.createRoot(document.getElementById('root')!);
root.render(
  <React.StrictMode>
    <ChemistryTool />
  </React.StrictMode>
);

